services:
  api:
    build: .
    container_name: astronomy-data-api
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    command: uv run uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload